program test 
defines {
	function test uses (integers val) catchable
	defines {
		let index be integer
		while (index smaller size val) 
		do {
			print val[index]
			index assign index plus 1
		}
		print "end"
	}
		
	do {
		call test with ([7,8,9])
	} catch {
		print "fail"
	}
}
