program test 
defines {
	let global x be integer 
	
	function test uses (integers val) catchable
	defines {
		let index be integer
		while (index smaller 10) 
		do {
			comment print index
			index assign index plus 2
		}
		print "end"
	}
		
	do {
		call test with ([1,2,3])
	} catch {
		print "fail"
	}
}
